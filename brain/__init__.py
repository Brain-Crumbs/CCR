"""Brain: the organism's learned components (V2 biological redesign).

Per-phase promotion out of ``cognitive_runtime`` behind re-export shims --
see ``docs/v2/phases/README.md``. The rename/regrouping of the rest of the
package tree happens after Phase 2 (the cortex) proves out, not before.
"""
