{
  "name": "ccr-mineflayer-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Line-delimited JSON bridge between the Continuous Cognitive Runtime's RemoteMinecraftBackend and a live Minecraft server via mineflayer.",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "check": "node --check index.js && node --check world.js && node --check actions.js && node --check observation.js && node --check blocks.js && node --check pixels.js"
  },
  "dependencies": {
    "mineflayer": "^4.20.0",
    "mineflayer-pathfinder": "^2.4.5",
    "vec3": "^0.1.10"
  },
  "optionalDependencies": {
    "node-canvas-webgl": "github:PrismarineJS/node-canvas-webgl",
    "prismarine-viewer": "^1.29.0"
  },
  "engines": {
    "node": ">=18"
  }
}
