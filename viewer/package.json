{
  "name": "ccr-clinic",
  "version": "0.2.0",
  "private": true,
  "description": "Zero-dependency Node/HTTP read-only API over streams-v2 Record sessions and Model Factory runs (server.js), plus a React presentation layer (web/) built into public/.",
  "license": "MIT",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js",
    "test": "node --test test/*.test.js",
    "build": "npm --prefix web run build",
    "dev": "npm --prefix web run dev",
    "test:web": "npm --prefix web run test"
  },
  "engines": {
    "node": ">=18"
  }
}
